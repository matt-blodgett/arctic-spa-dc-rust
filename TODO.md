# TODO

- [x] add a "discover" command for finding hot tub ips on the local network
- [x] fix "config" command issues and expand on it
- [x] add a "poll" command that creates a sqlite database and updates it at a set rate
- [x] add a mock up tbp server that receives and responds to messages similiarly to a hot tub
- [x] update "query" command to output json
- [x] <s/>fully implement the global "test" flag</s>
- [ ] don't need "Filter" messages?
- [ ] log heartbeats when polling?
- [x] remove unused imports code guards
- [x] add code prettier
- [x] add code test coverage
