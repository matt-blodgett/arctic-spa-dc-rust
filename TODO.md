# TODO

- [x] add a "discover" command for finding hot tub ips on the local network
- [x] fix "config" command issues and expand on it
- [ ] add a "poll" command that creates a sqlite database and updates it at a set rate
- [ ] add a mock up tbp server that receives and responds to messages similiarly to a hot tub
- [ ] update "query" command to output json
- [ ] <s/>fully implement the global "test" flag</s>
- [ ] don't need "Filter" message
- [ ] log heartbeats when polling?
- [ ] remove unused imports and dead code guards
- [ ] add code prettier
- [ ] add code test coverage